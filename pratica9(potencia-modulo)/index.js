const numero1 = Number(prompt('Digite um numero: '));
const numero2 = Number(prompt('Digite um numero: '));
const multi = numero1 ** numero2;
const div = numero1 % numero2;

alert(`Potência: ${multi}`);
alert(`Resto: ${div}`);
